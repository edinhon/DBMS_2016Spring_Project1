CREATE TABLE Article (
	articleId int PRIMARY KEY,
	title varchar(20),
	author varchar(20),
	view int
);

SELECT a.Article
FROM Article As a
WHERE title = 'SAO' AND author = 'billywithbelly' AND view = 1;

SELECT bookId, title, pages, authorId, editorial FROM Book;